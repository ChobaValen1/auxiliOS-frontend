/* AuxiliOS · Facturas · mesa administrativa canónica */
(()=>{'use strict';
const I=window.OperatorInvoices=window.OperatorInvoices||{};
const S=I.S={rows:[],filters:{companies:[],periods:[]},search:'',company:'',period:'',loading:false,detail:null,detailLoading:false,action:null,actionBusy:false,searchTimer:null};
const PDF_BUCKET='operator-invoice-pdfs';
const norm=v=>String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim();
const profile=()=>typeof PERFIL_USUARIO!=='undefined'?PERFIL_USUARIO:(window.PERFIL_USUARIO||{});
const role=()=>norm(profile()?.roles?.name||profile()?.role?.name||profile()?.role||profile()?.role_name||'');
const canRead=()=>['administracion','facturacion','supervision'].includes(role());
const canManage=()=>['administracion','facturacion'].includes(role());
const db=()=>typeof _db!=='undefined'?_db:null;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const num=v=>Number.isFinite(Number(v))?Number(v):0;
const notify=(m,t='info')=>typeof window.toast==='function'?window.toast(m,t):console[t==='error'?'error':'log'](m);
const money=(v,c='ARS')=>new Intl.NumberFormat('es-AR',{style:'currency',currency:c||'ARS',maximumFractionDigits:2}).format(num(v));
const date=v=>v?new Date(v).toLocaleString('es-AR',{timeZone:'America/Argentina/Buenos_Aires',day:'2-digit',month:'2-digit',year:'2-digit',hour:'2-digit',minute:'2-digit'}):'—';
function todayLocalDate(){const p=new Intl.DateTimeFormat('en-CA',{timeZone:'America/Argentina/Buenos_Aires',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(new Date()),g=t=>p.find(x=>x.type===t)?.value||'';return`${g('year')}-${g('month')}-${g('day')}`;}
function invoiceDate(value,fallback){const raw=String(value||'').slice(0,10),m=raw.match(/^(\d{4})-(\d{2})-(\d{2})$/);return m?`${m[3]}/${m[2]}/${m[1]}`:date(fallback);}
function periodLabel(value){if(!value)return'Todos los períodos';const [y,m]=value.split('-').map(Number);if(!y||!m)return value;return new Intl.DateTimeFormat('es-AR',{month:'long',year:'numeric',timeZone:'UTC'}).format(new Date(Date.UTC(y,m-1,1))).replace(/^./,x=>x.toUpperCase());}
function periodBounds(value){if(!value)return{start:null,end:null};const [y,m]=value.split('-').map(Number);if(!y||!m)return{start:null,end:null};const pad=n=>String(n).padStart(2,'0'),last=new Date(Date.UTC(y,m,0)).getUTCDate();return{start:`${y}-${pad(m)}-01`,end:`${y}-${pad(m)}-${pad(last)}`};}
function cleanFilePart(v){return String(v||'Factura').normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-zA-Z0-9_-]+/g,'_').replace(/^_+|_+$/g,'').slice(0,80)||'Factura';}
function injectAssets(){if(!document.getElementById('auxilios-operator-invoices-css')){const l=document.createElement('link');l.id='auxilios-operator-invoices-css';l.rel='stylesheet';l.href='/operator-invoices.css';document.head.appendChild(l)}}
function ensureShell(){injectAssets();let screen=document.getElementById('screen-facturas');if(!screen){const content=document.querySelector('.content');if(!content)return null;screen=document.createElement('div');screen.id='screen-facturas';screen.className='screen oi-screen';content.appendChild(screen)}let nav=document.getElementById('nav-facturas');if(!nav){const sidenav=document.querySelector('.sidenav'),billingNav=document.getElementById('nav-facturacion'),bottom=sidenav?.querySelector('.nav-bottom');if(sidenav){nav=document.createElement('div');nav.id='nav-facturas';nav.className='nav-item';nav.innerHTML='<span class="nav-icon">▤</span><span class="nav-label">Facturas</span>';nav.addEventListener('click',()=>open());if(billingNav?.parentNode===sidenav)sidenav.insertBefore(nav,billingNav.nextSibling);else if(bottom)sidenav.insertBefore(nav,bottom);else sidenav.appendChild(nav)}}if(nav)nav.style.display=canRead()?'':'none';if(!screen.dataset.boundOi){screen.dataset.boundOi='1';screen.addEventListener('click',onClick);screen.addEventListener('input',onInput);screen.addEventListener('change',onChange)}return screen;}
function setTopbar(){const title=document.getElementById('topbar-title'),sub=document.getElementById('topbar-sub');if(title)title.textContent='FACTURAS';if(sub)sub.textContent='Comprobantes · archivos · notas de crédito';}
async function open(invoiceId=null){if(!canRead())return notify('Sin permiso para Facturas','error');const screen=ensureShell();if(!screen)return;window.dispatchEvent(new CustomEvent('auxilios:navigation-changed',{detail:{screen:'facturas'}}));document.querySelectorAll('.screen').forEach(x=>x.classList.remove('active'));document.querySelectorAll('.nav-item').forEach(x=>x.classList.remove('active'));screen.classList.add('active');document.getElementById('nav-facturas')?.classList.add('active');setTopbar();S.detail=null;S.action=null;render();await load();if(invoiceId)await openDetail(invoiceId);}
function filterOptions(){const companies=['<option value="">Todas las prestadoras</option>',...S.filters.companies.map(x=>`<option value="${esc(x.company_id)}" ${String(S.company)===String(x.company_id)?'selected':''}>${esc(x.company_name)}</option>`)].join('');const periods=['<option value="">Todos los períodos</option>',...S.filters.periods.map(p=>`<option value="${esc(p)}" ${S.period===p?'selected':''}>${esc(periodLabel(p))}</option>`)].join('');return{companies,periods};}
function statusLabel(v){return v==='cancelled'?'ANULADA':v==='credited'?'ACREDITADA':'FACTURADA';}
function statusClass(v){return v==='cancelled'?'cancelled':v==='credited'?'credited':'';}
function rowById(id){return S.rows.find(r=>String(r.invoice_id)===String(id))||null;}
function creditType(invoiceType){return invoiceType==='FA'?'NCA':invoiceType==='FB'?'NCB':invoiceType==='FC'?'NCC':'';}
function creditTypeLabel(v){return v==='NCA'?'Nota de Crédito A':v==='NCB'?'Nota de Crédito B':v==='NCC'?'Nota de Crédito C':'Nota de Crédito';}
function creditNumber(c){if(!c)return'';return`${creditTypeLabel(c.document_type)} ${c.point_of_sale||''}-${c.document_number||''}`.trim();}
function closeActionMenu(){document.querySelectorAll('[data-oi-menu][aria-expanded="true"]').forEach(x=>x.setAttribute('aria-expanded','false'));document.getElementById('oi-action-menu')?.remove();}
function actionMenuMarkup(row){const active=row.status==='created',pdf=row.pdf_path;return`${pdf?`<button type="button" data-oi-row-action="view-pdf" data-invoice-id="${esc(row.invoice_id)}">Ver PDF</button><button type="button" data-oi-row-action="attach-pdf" data-invoice-id="${esc(row.invoice_id)}">Reemplazar PDF</button>`:`<button type="button" data-oi-row-action="attach-pdf" data-invoice-id="${esc(row.invoice_id)}">Adjuntar PDF</button>`}<button type="button" data-oi-row-action="excel" data-invoice-id="${esc(row.invoice_id)}">Descargar Excel</button>${canManage()&&active?`<button type="button" data-oi-row-action="credit-note" data-invoice-id="${esc(row.invoice_id)}">Emitir Nota de Crédito</button><button type="button" class="danger" data-oi-row-action="annul" data-invoice-id="${esc(row.invoice_id)}">Anular</button>`:''}`;}
function toggleActionMenu(trigger,id){const existing=document.getElementById('oi-action-menu');if(existing?.dataset.invoiceId===String(id)){closeActionMenu();return}const row=rowById(id);if(!row)return;closeActionMenu();const menu=document.createElement('div');menu.id='oi-action-menu';menu.className='oi-action-menu';menu.dataset.invoiceId=String(id);menu.innerHTML=actionMenuMarkup(row);document.body.appendChild(menu);const r=trigger.getBoundingClientRect(),margin=8,gap=6;let left=Math.max(margin,Math.min(r.right-menu.offsetWidth,window.innerWidth-menu.offsetWidth-margin)),top=r.bottom+gap;if(top+menu.offsetHeight>window.innerHeight-margin)top=Math.max(margin,r.top-menu.offsetHeight-gap);menu.style.left=`${left}px`;menu.style.top=`${top}px`;trigger.setAttribute('aria-expanded','true');}
function tableMarkup(){if(!S.rows.length)return'<div class="oi-empty">Todavía no hay facturas con estos filtros.</div>';return`<div class="oi-table-wrap"><table class="oi-table"><thead><tr><th>Factura</th><th>Fecha</th><th>Prestadora</th><th>Servicios</th><th>Total</th><th>Estado</th><th>PDF</th><th></th></tr></thead><tbody>${S.rows.map(r=>`<tr><td><b>${esc(r.invoice_number||'—')}</b>${r.credit_note_id?`<small>${esc(`${creditTypeLabel(r.credit_note_type)} ${r.credit_note_point_of_sale}-${r.credit_note_number}`)}</small>`:''}</td><td>${esc(invoiceDate(r.issued_on,r.created_at))}</td><td><b>${esc(r.company_name||'—')}</b></td><td>${esc(r.service_count||0)}</td><td><b class="oi-money">${esc(money(r.total_amount,r.currency))}</b></td><td><span class="oi-status ${statusClass(r.status)}">${esc(statusLabel(r.status))}</span></td><td>${r.pdf_path?'<span class="oi-file">PDF adjunto</span>':'<span class="oi-muted">Sin PDF</span>'}</td><td class="oi-actions"><button class="oi-button" type="button" data-oi-detail="${esc(r.invoice_id)}">Ver</button><button class="oi-menu-trigger" type="button" data-oi-menu="${esc(r.invoice_id)}" aria-haspopup="menu" aria-expanded="false" title="Acciones">⋯</button></td></tr>`).join('')}</tbody></table></div>`;}
function lineMarkup(line){const s=line.service_snapshot||{},q=line.quote_snapshot||{},released=!!line.released_at;return`<article class="oi-line ${released?'released':''}"><div class="oi-line-main"><b>${esc(s.service_order_number||s.service_number||'Servicio')}</b><small>${esc(date(s.scheduled_for))} · ${esc(s.customer_name||'Sin cliente')}</small><small>${esc([s.vehicle_make_model,s.vehicle_plate].filter(Boolean).join(' · ')||'Sin vehículo')}</small>${released?'<small class="oi-release">Liberado por anulación</small>':''}</div><div class="oi-line-route"><small>Origen</small><b>${esc(s.origin||'—')}</b><small>Destino</small><b>${esc(s.destination||'—')}</b></div><div class="oi-line-price"><small>Importe congelado</small><b>${esc(money(line.company_amount,line.currency))}</b><small>${q.rate_card_name?`${esc(q.rate_card_name)} · v${esc(q.rate_card_version||'—')}`:'Tarifa congelada'}</small></div></article>`;}
function detailMarkup(){const d=S.detail,i=d.invoice||{},lines=Array.isArray(d.lines)?d.lines:[],cn=d.credit_note||null;return`<aside class="oi-detail"><div class="oi-detail-head"><div><small>Factura · ${esc(statusLabel(i.status))}</small><h3>${esc(i.invoice_number||'Factura')}</h3></div><div class="oi-detail-head-actions">${canRead()?`<button class="oi-menu-trigger" type="button" data-oi-menu="${esc(i.invoice_id)}" aria-haspopup="menu" aria-expanded="false">Acciones ⋯</button>`:''}<button class="oi-button" type="button" data-oi="close-detail">× Cerrar</button></div></div><div class="oi-detail-body"><div class="oi-summary"><article><small>Prestadora</small><b>${esc(i.company_name||'—')}</b></article><article><small>Servicios</small><b>${esc(i.service_count||0)}</b></article><article><small>Total</small><b>${esc(money(i.total_amount,i.currency))}</b></article></div><section class="oi-section"><h4>Datos de factura</h4><div class="oi-grid"><div><small>Fecha de emisión</small><b>${esc(invoiceDate(i.issued_on,i.created_at))}</b></div><div><small>Creada por</small><b>${esc(i.created_by_name||'Usuario')}</b></div><div><small>Estado</small><b>${esc(statusLabel(i.status))}</b></div><div><small>Moneda</small><b>${esc(i.currency||'ARS')}</b></div><div><small>PDF</small><b>${i.pdf_path?esc(i.pdf_name||'Adjunto'):'Sin PDF'}</b></div>${i.notes?`<div><small>Observaciones</small><b>${esc(i.notes)}</b></div>`:''}${i.cancellation_reason?`<div class="oi-wide"><small>Motivo de anulación</small><b>${esc(i.cancellation_reason)}</b></div>`:''}</div></section>${cn?`<section class="oi-section oi-credit"><h4>Nota de Crédito</h4><div class="oi-grid"><div><small>Comprobante</small><b>${esc(creditNumber(cn))}</b></div><div><small>Fecha</small><b>${esc(invoiceDate(cn.issued_on,cn.created_at))}</b></div><div><small>Importe</small><b>${esc(money(cn.amount,cn.currency))}</b></div><div><small>Alcance</small><b>Total</b></div></div></section>`:''}<section class="oi-section"><h4>Servicios facturados</h4><div class="oi-lines">${lines.length?lines.map(lineMarkup).join(''):'<div class="oi-empty">Sin servicios.</div>'}</div></section></div></aside>`;}
function actionModalMarkup(){const a=S.action;if(!a)return'';const row=rowById(a.invoiceId)||S.detail?.invoice||{},busy=S.actionBusy;if(a.type==='annul')return`<section class="oi-modal" role="dialog" aria-modal="true"><header><div><small>Factura</small><h3>Anular ${esc(row.invoice_number||'factura')}</h3></div><button class="oi-button" data-oi="close-action" ${busy?'disabled':''}>×</button></header><div class="oi-modal-body"><div class="oi-warning"><b>Se devolverán ${esc(row.service_count||0)} servicios a Facturación.</b><span>La factura y sus líneas permanecen en el historial. Esta acción no anula fiscalmente el comprobante ante ARCA.</span></div><label><span>Motivo de anulación</span><input data-oi-action-field="reason" maxlength="300" placeholder="Motivo obligatorio" value="${esc(a.form.reason||'')}"></label></div><footer><button class="oi-button" data-oi="close-action" ${busy?'disabled':''}>Cancelar</button><button class="oi-button danger" data-oi="confirm-annul" ${busy?'disabled':''}>${busy?'Anulando…':'Anular y devolver servicios'}</button></footer></section>`;if(a.type==='credit-note'){const t=creditType(row.document_type);return`<section class="oi-modal" role="dialog" aria-modal="true"><header><div><small>${esc(row.invoice_number||'Factura')}</small><h3>Emitir Nota de Crédito</h3></div><button class="oi-button" data-oi="close-action" ${busy?'disabled':''}>×</button></header><div class="oi-modal-body"><div class="oi-credit-total"><small>Nota de Crédito total</small><b>${esc(money(row.total_amount,row.currency))}</b><span>Los servicios permanecen vinculados a la factura original.</span></div><div class="oi-modal-grid"><label><span>Comprobante</span><input value="${esc(creditTypeLabel(t))}" disabled></label><label><span>Punto de venta</span><input data-oi-action-field="point_of_sale" inputmode="numeric" maxlength="10" placeholder="0004" value="${esc(a.form.point_of_sale||'')}"></label><label><span>Número</span><input data-oi-action-field="document_number" inputmode="numeric" maxlength="20" placeholder="00000125" value="${esc(a.form.document_number||'')}"></label><label><span>Fecha</span><input type="date" data-oi-action-field="issued_on" value="${esc(a.form.issued_on||todayLocalDate())}"></label></div><label><span>Observaciones <small>opcional</small></span><input data-oi-action-field="notes" maxlength="300" placeholder="Referencia breve" value="${esc(a.form.notes||'')}"></label></div><footer><button class="oi-button" data-oi="close-action" ${busy?'disabled':''}>Cancelar</button><button class="oi-button primary" data-oi="confirm-credit-note" ${busy?'disabled':''}>${busy?'Emitiendo…':'Emitir Nota de Crédito'}</button></footer></section>`;return'';}
function render(){const screen=ensureShell();if(!screen)return;closeActionMenu();const opts=filterOptions(),detailOpen=S.detail||S.detailLoading,actionOpen=!!S.action;screen.innerHTML=`<div class="oi-shell"><div class="oi-toolbar"><div class="oi-filters"><input class="oi-search" id="oi-search" placeholder="Buscar factura, prestadora o servicio…" value="${esc(S.search)}"><select class="oi-filter" id="oi-company-filter">${opts.companies}</select><select class="oi-filter" id="oi-period-filter">${opts.periods}</select><button class="oi-button" type="button" data-oi="refresh">↻ Actualizar</button></div></div><div class="oi-table-card">${S.loading?'<div class="oi-empty">Actualizando Facturas…</div>':tableMarkup()}</div><div class="oi-detail-backdrop" ${detailOpen?'':'hidden'}>${S.detailLoading?'<aside class="oi-detail"><div class="oi-empty">Cargando factura…</div></aside>':S.detail?detailMarkup():''}</div><div class="oi-modal-backdrop" ${actionOpen?'':'hidden'}>${actionOpen?actionModalMarkup():''}</div></div>`;}
async function load(){if(S.loading||!db()||!canRead())return;S.loading=true;render();const bounds=periodBounds(S.period);try{const {data,error}=await db().rpc('list_operator_invoices_v2',{p_search:S.search||null,p_company_id:S.company||null,p_period_start:bounds.start,p_period_end:bounds.end});if(error)throw error;S.rows=Array.isArray(data?.rows)?data.rows:[];S.filters={companies:Array.isArray(data?.filters?.companies)?data.filters.companies:[],periods:Array.isArray(data?.filters?.periods)?data.filters.periods:[]}}catch(e){S.rows=[];notify(e.message||'No se pudieron cargar las Facturas','error')}finally{S.loading=false;render()}}
async function fetchDetail(id){const {data,error}=await db().rpc('get_operator_invoice_detail_v2',{p_invoice_id:id});if(error)throw error;return data;}
async function openDetail(id){if(!id||S.detailLoading)return;S.detail=null;S.detailLoading=true;render();try{S.detail=await fetchDetail(id)}catch(e){S.detail=null;notify(e.message||'No se pudo abrir la factura','error')}finally{S.detailLoading=false;render()}}
function closeDetail(){S.detail=null;S.detailLoading=false;render();}
function openAnnul(id){const row=rowById(id);if(!canManage())return notify('Sin permiso para anular facturas','error');if(!row||row.status!=='created')return notify('Sólo se puede anular una factura activa','warning');S.action={type:'annul',invoiceId:id,form:{reason:''}};render();}
function openCreditNote(id){const row=rowById(id);if(!canManage())return notify('Sin permiso para emitir Notas de Crédito','error');if(!row||row.status!=='created')return notify('La factura ya no admite una Nota de Crédito','warning');const type=creditType(row.document_type);if(!type)return notify('El tipo de factura no admite Nota de Crédito','warning');S.action={type:'credit-note',invoiceId:id,form:{document_type:type,point_of_sale:row.point_of_sale||'',document_number:'',issued_on:todayLocalDate(),notes:''}};render();}
function closeAction(){if(S.actionBusy)return;S.action=null;render();}
async function confirmAnnul(){const a=S.action,row=rowById(a?.invoiceId);if(!a||a.type!=='annul'||!row||S.actionBusy)return;const reason=String(a.form.reason||'').trim();if(reason.length<3)return notify('Ingresá un motivo de anulación','warning');S.actionBusy=true;render();try{const {data,error}=await db().rpc('annul_operator_invoice_v2',{p_invoice_id:a.invoiceId,p_reason:reason});if(error)throw error;S.action=null;S.detail=null;notify(`Factura anulada · ${data?.released_service_count||row.service_count||0} servicios volvieron a Facturación`,'success');await load()}catch(e){notify(e.message||'No se pudo anular la factura','error')}finally{S.actionBusy=false;render()}}
async function confirmCreditNote(){const a=S.action,row=rowById(a?.invoiceId);if(!a||a.type!=='credit-note'||!row||S.actionBusy)return;const f=a.form;if(!/^\d+$/.test(String(f.point_of_sale||'')))return notify('Ingresá el punto de venta usando sólo números','warning');if(!/^\d+$/.test(String(f.document_number||'')))return notify('Ingresá el número usando sólo números','warning');if(!/^\d{4}-\d{2}-\d{2}$/.test(String(f.issued_on||'')))return notify('Ingresá una fecha válida','warning');S.actionBusy=true;render();try{const {error}=await db().rpc('create_operator_invoice_credit_note_v1',{p_invoice_id:a.invoiceId,p_document_type:f.document_type,p_point_of_sale:f.point_of_sale.trim(),p_document_number:f.document_number.trim(),p_issued_on:f.issued_on,p_notes:String(f.notes||'').trim()||null});if(error)throw error;S.action=null;notify('Nota de Crédito total registrada','success');await load();await openDetail(row.invoice_id)}catch(e){notify(e.message||'No se pudo emitir la Nota de Crédito','error')}finally{S.actionBusy=false;render()}}
async function choosePdf(id){if(!canManage())return notify('Sin permiso para adjuntar PDF','error');const row=rowById(id);if(!row)return;const input=document.createElement('input');input.type='file';input.accept='application/pdf,.pdf';input.hidden=true;document.body.appendChild(input);input.addEventListener('change',async()=>{const file=input.files?.[0];input.remove();if(!file)return;if(file.type&&file.type!=='application/pdf')return notify('Seleccioná un archivo PDF','warning');if(file.size>15*1024*1024)return notify('El PDF no puede superar 15 MB','warning');const path=`${id}/factura.pdf`;notify('Subiendo PDF…');try{const storage=db()?.storage?.from(PDF_BUCKET);if(!storage)throw new Error('Storage no está disponible');const {error:uploadError}=await storage.upload(path,file,{upsert:true,contentType:'application/pdf',cacheControl:'3600'});if(uploadError)throw uploadError;const {error:linkError}=await db().rpc('attach_operator_invoice_pdf_v1',{p_invoice_id:id,p_pdf_path:path,p_pdf_name:file.name});if(linkError)throw linkError;notify('PDF adjuntado a la factura','success');await load();if(S.detail?.invoice?.invoice_id===id)await openDetail(id)}catch(e){notify(e.message||'No se pudo adjuntar el PDF','error')}});document.body.appendChild(input);input.click();}
async function viewPdf(id){const row=rowById(id)||S.detail?.invoice;if(!row?.pdf_path)return notify('La factura no tiene PDF adjunto','warning');try{const storage=db()?.storage?.from(PDF_BUCKET);if(!storage)throw new Error('Storage no está disponible');const {data,error}=await storage.createSignedUrl(row.pdf_path,3600);if(error)throw error;if(!data?.signedUrl)throw new Error('No se pudo generar el acceso al PDF');window.open(data.signedUrl,'_blank','noopener,noreferrer')}catch(e){notify(e.message||'No se pudo abrir el PDF','error')}}
async function exportExcel(id){try{const detail=S.detail?.invoice?.invoice_id===id?S.detail:await fetchDetail(id),invoice=detail?.invoice||{},lines=Array.isArray(detail?.lines)?detail.lines:[],excel=window.AuxiliosExcelExport;if(!excel)throw new Error('El exportador Excel todavía no está disponible');excel.ensureReady?.();const serviceColumns=[{header:'Línea',width:9,key:'line_number',type:'number'},{header:'N° Orden',width:18,key:'order'},{header:'Servicio',width:18,key:'service_number'},{header:'Fecha',width:14,key:'scheduled_for'},{header:'Cliente',width:24,key:'customer'},{header:'Vehículo',width:24,key:'vehicle'},{header:'Patente',width:14,key:'plate'},{header:'Origen',width:40,key:'origin'},{header:'Destino',width:40,key:'destination'},{header:'Importe',width:16,key:'amount',type:'number'},{header:'Moneda',width:10,key:'currency'},{header:'Estado vínculo',width:16,key:'link_status'}],serviceRows=lines.map(l=>{const s=l.service_snapshot||{};return{line_number:l.line_number,order:s.service_order_number||'',service_number:s.service_number||'',scheduled_for:invoiceDate(String(s.scheduled_for||'').slice(0,10),s.scheduled_for),customer:s.customer_name||'',vehicle:s.vehicle_make_model||'',plate:s.vehicle_plate||'',origin:s.origin||'',destination:s.destination||'',amount:num(l.company_amount),currency:l.currency||invoice.currency||'ARS',link_status:l.released_at?'Liberado por anulación':'Facturado'}}),summaryColumns=[{header:'Dato',width:28,key:'label'},{header:'Valor',width:30,key:'value'}],summaryRows=[{label:'Factura',value:invoice.invoice_number||''},{label:'Prestadora',value:invoice.company_name||''},{label:'Fecha emisión',value:invoiceDate(invoice.issued_on,invoice.created_at)},{label:'Estado',value:statusLabel(invoice.status)},{label:'Servicios',value:String(invoice.service_count||lines.length)},{label:'Total',value:num(invoice.total_amount)},{label:'Moneda',value:invoice.currency||'ARS'}];if(detail.credit_note)summaryRows.push({label:'Nota de Crédito',value:creditNumber(detail.credit_note)},{label:'Importe NC',value:num(detail.credit_note.amount)});excel.download({filename:`AuxiliOS_${cleanFilePart(invoice.invoice_number||'Factura')}`,sheets:[{name:'Resumen',columns:summaryColumns,rows:summaryRows},{name:'Servicios',columns:serviceColumns,rows:serviceRows}]});notify('Excel de la factura descargado','success')}catch(e){notify(e.message||'No se pudo descargar el Excel','error')}}
function handleRowAction(action,id){if(action==='attach-pdf')return choosePdf(id);if(action==='view-pdf')return viewPdf(id);if(action==='excel')return exportExcel(id);if(action==='credit-note')return openCreditNote(id);if(action==='annul')return openAnnul(id);}
function onInput(e){const field=e.target.dataset?.oiActionField;if(field&&S.action){if(['point_of_sale','document_number'].includes(field)){const digits=e.target.value.replace(/\D/g,'');e.target.value=digits;S.action.form[field]=digits}else S.action.form[field]=e.target.value;return}if(e.target.id!=='oi-search')return;S.search=e.target.value;clearTimeout(S.searchTimer);S.searchTimer=setTimeout(load,300);}
function onChange(e){const field=e.target.dataset?.oiActionField;if(field&&S.action){S.action.form[field]=e.target.value;return}if(e.target.id==='oi-company-filter'){S.company=e.target.value||'';return load()}if(e.target.id==='oi-period-filter'){S.period=e.target.value||'';return load()}}
function onClick(e){const menu=e.target.closest('[data-oi-menu]');if(menu){e.stopPropagation();return toggleActionMenu(menu,menu.dataset.oiMenu)}const detail=e.target.closest('[data-oi-detail]');if(detail)return openDetail(detail.dataset.oiDetail);const action=e.target.closest('[data-oi]')?.dataset.oi;if(action==='refresh')return load();if(action==='close-detail')return closeDetail();if(action==='close-action')return closeAction();if(action==='confirm-annul')return confirmAnnul();if(action==='confirm-credit-note')return confirmCreditNote();}
function onDocumentClick(e){const action=e.target.closest('[data-oi-row-action]');if(action){e.preventDefault();const type=action.dataset.oiRowAction,id=action.dataset.invoiceId;closeActionMenu();return handleRowAction(type,id)}if(!e.target.closest('[data-oi-menu]')&&!e.target.closest('#oi-action-menu'))closeActionMenu();}
function init(){ensureShell();document.addEventListener('click',onDocumentClick);document.addEventListener('scroll',closeActionMenu,true);window.addEventListener('resize',closeActionMenu);let attempts=0;const timer=setInterval(()=>{const nav=document.getElementById('nav-facturas');if(nav)nav.style.display=canRead()?'':'none';if(canRead()&&db())clearInterval(timer);else if(++attempts>120)clearInterval(timer)},100);}
Object.assign(I,{open,load,render,openDetail,canRead,canManage,exportExcel});
Object.assign(window,{abrirFacturasOperador:open,cargarFacturasOperador:load});
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init,{once:true}):init();
})();