{
  "name": "sigma-choferes",
  "version": "1.0.0",
  "description": "Gestión de flota y remitos digitales — Sigma Remolques",
  "private": true,
  "scripts": {
    "check": "node --check sigma.js && node --check empresas.js && node --check supabase.js && node --check offline.js && node --check sw.js && node scripts/security-check.js",
    "test": "node --test test/*.test.js",
    "ci": "npm run check && npm test",
    "diagnose-users": "node diagnose-users.js",
    "fix-auth-users": "node fix-auth-users.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.0"
  }
}
