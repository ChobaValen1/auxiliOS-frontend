/* AuxiliOS · Servicios · puente canónico Chofer → Remito v2 */
(()=>{'use strict';
const P3=window.AuxiliosPhase3=window.AuxiliosPhase3||{queue:[],history:[],loading:false,timer:null,selectedServiceId:null,initialized:false,view:'active',activationInFlight:false,signedEditData:null,correctionInFlight:false};
const STATUS={assigned:{label:'Asignado',icon:'●',tone:'blue'},at_origin:{label:'Arribado',icon:'⌖',tone:'violet'},completed:{label:'Finalizado',icon:'✓',tone:'green'},cancelled:{label:'Anulado',icon:'×',tone:'red'}};
const ACTIVATION_REASONS=[
 ['absent_or_not_towable','Cancelado por socio ausente / vehículo no apto'],
 ['provider','Cancelado por Prestadora'],
 ['us','Cancelado por nosotros'],
 ['other','Otro']
];
const PAYMENT_LABELS={cash:'Efectivo',transfer:'Transferencia',card:'Tarjeta',mercado_pago:'Mercado Pago',other:'Otro',not_collected:'No cobrado'};
const TOLL_COVERAGE_LABELS={mixed_manual:'Uno y Uno',provider_roundtrip:'A cargo de la prestadora',customer_roundtrip:'A cargo del cliente'};
const EXTRA_FIELDS={
 'Email del cliente':{key:'customer_email',id:'phase3-service-email',label:'Email del cliente',type:'email'},
 'Observaciones':{key:'operator_notes',id:'phase3-service-operator-notes',label:'Observaciones del servicio',type:'textarea'},
 'Indicaciones para el chofer':{key:'driver_instructions',id:'phase3-service-driver-instructions',label:'Indicaciones para el chofer',type:'textarea'},
 'Orden de compra':{key:'purchase_order_number',id:'phase3-service-purchase-order',label:'Orden de compra',type:'text'}
};
const db=()=>typeof _db!=='undefined'?_db:null;
const profile=()=>typeof PERFIL_USUARIO!=='undefined'?PERFIL_USUARIO:null;
const role=()=>String(profile()?.roles?.name||profile()?.role||'').toLowerCase();
const isDriver=()=>role()==='chofer';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));
const notify=(m,t='info')=>typeof window.toast==='function'?window.toast(m,t):console[t==='error'?'error':'log'](m);
const currentDate=()=>new Date().toLocaleDateString('es-AR',{day:'2-digit',month:'2-digit',year:'numeric'});
const money=value=>Number(value||0).toLocaleString('es-AR',{style:'currency',currency:'ARS',maximumFractionDigits:0});
const originDestinationKm=meters=>{
  const value=Number(meters);
  return Number.isFinite(value)&&value>0?`${(Math.round(value/100)/10).toLocaleString('es-AR')} km`:'Sin cálculo Maps';
};
const tollCoverageLabel=mode=>TOLL_COVERAGE_LABELS[String(mode||'')]||'Sin formato configurado';
function customerExcessSummary(s,{compact=false}={}){
  const data=s.customer_excess_summary||{};
  const total=Number(data.total_amount||s.customer_amount_due||0);
  const items=Array.isArray(data.items)?data.items.filter(Boolean):[];
  const count=Number(data.count||items.length||0);
  if(total<=0&&count<=0)return'';
  const first=items[0]||{};
  const firstName=first.concept_name||'Excedente';
  const remaining=Math.max(0,count-1);
  return`<div class="p3-summary-block p3-customer-excess ${compact?'compact':''}"><div class="p3-summary-line"><span>Excedentes</span><strong>${esc(firstName)} · ${esc(money(first.total_amount||total))}</strong></div>${remaining?`<div class="p3-summary-line muted"><span></span><strong>+${remaining} más · ${esc(money(total))}</strong></div>`:''}</div>`;
}
function tollSummary(s,{compact=false}={}){
  const data=s.toll_charge_summary||{};
  const groups=Array.isArray(data.groups)?data.groups.filter(group=>Number(group.count||0)>0):[];
  if(!groups.length)return'';
  const labels={customer:'A cargo del socio',provider:'A cargo de la prestadora',mixed:'Mixto'};
  const rows=groups.map(group=>{
    const items=Array.isArray(group.items)?group.items.filter(Boolean):[];
    const first=items[0]||{};
    const extra=Math.max(0,Number(group.count||items.length||0)-1);
    const amount=Number(first.amount||group.total_amount||0);
    const suffix=extra?` (+${extra} más)`:'';
    return`<div class="p3-summary-line"><span>${esc(labels[group.key]||group.label||'Peajes')}</span><strong>${esc(first.toll_name||'Peaje')} · ${esc(money(amount))}${suffix}</strong></div>`;
  }).join('');
  return`<div class="p3-summary-block p3-toll-summary ${compact?'compact':''}"><div class="p3-summary-title">Peajes</div>${rows}</div>`;
}
function serviceFacts(s,{compact=false}={}){
  const vehicle=s.vehicle_make_model||'Vehículo sin informar',plate=s.vehicle_plate||'Sin patente';
  return`<div class="p3-facts"><div class="p3-fact-row p3-vehicle-fact"><span>Vehículo</span><strong>${esc(vehicle)} <em>${esc(plate)}</em></strong></div><div class="p3-fact-row"><span>Origen</span><strong>${esc(s.origin||'—')}</strong></div><div class="p3-fact-row"><span>Destino</span><strong>${esc(s.destination||'—')}</strong></div><div class="p3-fact-row"><span>KM</span><strong>${esc(originDestinationKm(s.origin_destination_distance_meters))}</strong></div><div class="p3-fact-row p3-toll-coverage-fact"><span>Formato de cobro de peajes</span><strong>${esc(tollCoverageLabel(s.toll_coverage_mode))}</strong></div>${customerExcessSummary(s,{compact})}${tollSummary(s,{compact})}</div>`;
}
function injectAssets(){if(document.getElementById('phase3-service-bridge-css'))return;const l=document.createElement('link');l.id='phase3-service-bridge-css';l.rel='stylesheet';l.href='/operator-service-bridge.css';document.head.appendChild(l)}
function applyDriverModuleLabels(){if(!isDriver())return;const nav=document.getElementById('nav-remitos'),screen=document.getElementById('screen-remitos');const label=nav?.querySelector('.nav-label'),icon=nav?.querySelector('.nav-icon');if(label)label.textContent='Servicios';if(icon)icon.textContent='📡';if(screen?.classList.contains('active')){const title=document.getElementById('topbar-title'),sub=document.getElementById('topbar-sub');if(title)title.textContent='SERVICIOS';if(sub)sub.textContent='Asignados e historial'}}
function hookNavigation(){if(window.__p3DriverNavHook||typeof window.goTo!=='function')return false;const base=window.goTo;window.goTo=(name,...args)=>{const result=base(name,...args);if(name==='remitos')applyDriverModuleLabels();return result};window.__p3DriverNavHook=true;return true}
function ensurePreviewModal(){if(document.getElementById('p3-service-preview'))return;document.body.insertAdjacentHTML('beforeend',`<div id="p3-service-preview" class="p3-preview" hidden><button type="button" class="p3-preview-backdrop" onclick="cerrarPreviewServicio()" aria-label="Cerrar"></button><section role="dialog" aria-modal="true" aria-labelledby="p3-preview-title"><header><div><small>Servicio asignado</small><h2 id="p3-preview-title">Servicio</h2></div><button type="button" onclick="cerrarPreviewServicio()" aria-label="Cerrar">×</button></header><div id="p3-preview-body"></div><footer id="p3-preview-actions"></footer></section></div>`)}
function injectPanel(){if(!isDriver()||document.getElementById('phase3-driver-services'))return;const remitos=document.getElementById('screen-remitos');if(!remitos)return;remitos.classList.add('p3-driver-remitos');const header=remitos.querySelector('.sec-header'),anchor=header||remitos;anchor.insertAdjacentHTML(header?'afterend':'afterbegin',`<section id="phase3-driver-services" class="p3-driver-panel p3-remitos-assigned" data-location="remitos" aria-live="polite"><div class="p3-panel-head"><div class="p3-head-actions"><button type="button" class="p3-refresh" onclick="abrirRemitoSinAsignacion()">＋ Sin asignación</button><button id="p3-view-active" type="button" class="p3-refresh" onclick="cambiarVistaServiciosChofer('active')">Activos</button><button id="p3-view-history" type="button" class="p3-refresh" onclick="cambiarVistaServiciosChofer('history')">Historial</button><button type="button" class="p3-refresh" onclick="actualizarServiciosAsignados()" aria-label="Actualizar">↻</button></div></div><div id="phase3-driver-services-list" class="p3-service-list"><div class="p3-empty">Buscando servicios…</div></div></section>`);ensurePreviewModal();applyDriverModuleLabels()}
async function loadQueue({silent=false}={}){if(!isDriver()||P3.loading||!db())return;if(P3.view==='history')return render();P3.loading=true;try{const {data,error}=await db().rpc('get_driver_operator_queue_v3');if(error)throw error;P3.queue=Array.isArray(data)?data:[];render()}catch(e){if(!silent)notify(e.message||'No se pudieron cargar los servicios','error');const list=document.getElementById('phase3-driver-services-list');if(list)list.innerHTML='<div class="p3-empty">No se pudo actualizar la información.</div>'}finally{P3.loading=false}}
function render(){injectPanel();applyDriverModuleLabels();const list=document.getElementById('phase3-driver-services-list'),panel=document.getElementById('phase3-driver-services'),screen=document.getElementById('screen-remitos');if(!list||!panel||!screen)return;const history=P3.view==='history';document.getElementById('p3-view-active')?.classList.toggle('active',!history);document.getElementById('p3-view-history')?.classList.toggle('active',history);screen.classList.toggle('p3-hide-remitos-archive',!history);panel.classList.toggle('p3-history-only',history);if(history){list.innerHTML='';return}const rows=P3.queue;panel.classList.toggle('has-services',rows.length>0);if(!rows.length){list.innerHTML='<div class="p3-empty"><span>✓</span>No tenés servicios activos asignados.</div>';return}list.innerHTML=rows.map(activeCard).join('')}
function activeCard(s){
  const meta=STATUS[s.status]||STATUS.assigned;
  const order=s.service_order_number||'Sin N° prestación';
  const action=`role="button" tabindex="0" data-service-id="${esc(s.service_id)}" aria-label="Ver prestación ${esc(order)}" onclick="abrirPreviewServicio(this.dataset.serviceId)" onkeydown="if(event.key==='Enter'||event.key===' '){event.preventDefault();abrirPreviewServicio(this.dataset.serviceId)}"`;
  return`<article class="p3-service-card is-actionable" ${action}>
    <div class="p3-service-bar">
      <div class="p3-bar-left">
        <span class="p3-bar-item"><span class="p3-field-label">Fecha</span><strong class="p3-date-value">${esc(currentDate())}</strong></span>
        <span class="p3-bar-divider" aria-hidden="true">|</span>
        <span class="p3-bar-item"><span class="p3-field-label">N° Prestación</span><strong class="p3-order-value">${esc(order)}</strong></span>
      </div>
      <div class="p3-status-field"><span class="p3-field-label">Estado</span><span class="p3-status ${meta.tone}">${esc(meta.label)}</span></div>
    </div>
    <div class="p3-compact-body"><div class="p3-company-line"><strong>${esc(s.company_name||'Empresa sin informar')}</strong><span aria-hidden="true">|</span><b>${esc(order)}</b></div>${serviceFacts(s,{compact:true})}</div>
  </article>`;
}
function closePreview(){const modal=document.getElementById('p3-service-preview');if(modal)modal.hidden=true;document.body.classList.remove('p3-preview-open')}
function previewBody(s){const order=s.service_order_number||'Sin N° prestación';return`<div class="p3-preview-facts"><div class="p3-company-line"><strong>${esc(s.company_name||'Empresa sin informar')}</strong><span>|</span><b>${esc(order)}</b></div>${serviceFacts(s)}</div>`}
function openPreview(id){const s=findService(id);if(!s)return notify('El servicio ya no está disponible','error');ensurePreviewModal();const modal=document.getElementById('p3-service-preview'),title=document.getElementById('p3-preview-title'),body=document.getElementById('p3-preview-body'),actions=document.getElementById('p3-preview-actions');title.textContent=s.service_order_number||'Sin N° prestación';body.innerHTML=previewBody(s);if(s.remito_action==='edit'||s.can_edit_remito){actions.innerHTML=`<button class="p3-preview-primary" type="button" data-id="${esc(id)}" onclick="cerrarPreviewServicio();abrirRemitoServicio(this.dataset.id)">Editar remito</button><small>Firmado. Sólo se pueden corregir cargos, evidencia y observaciones antes del cierre.</small>`}else if(s.remito_action==='complete'||s.can_complete_remito){actions.innerHTML=`<button class="p3-preview-primary" type="button" data-id="${esc(id)}" onclick="cerrarPreviewServicio();abrirRemitoServicio(this.dataset.id)">Completar remito</button><button class="p3-preview-secondary" type="button" data-id="${esc(id)}" onclick="confirmarServicioActivado(this.dataset.id)">Marcar como activado</button><small>Usar si el servicio fue cancelado o no se realizó.</small>`}else{actions.innerHTML='<small>El remito firmado está disponible en el historial.</small>'}modal.hidden=false;document.body.classList.add('p3-preview-open')}
function confirmActivated(id){
  const s=findService(id);if(!s)return;
  const body=document.getElementById('p3-preview-body'),actions=document.getElementById('p3-preview-actions');
  const detail=s.remito_id&&s.remito_status==='pendiente'?'El borrador guardado se anulará y el servicio se cerrará como cancelado o no realizado.':'El servicio se cerrará como cancelado o no realizado. No se generará un remito.';
  const choices=ACTIVATION_REASONS.map(([value,label])=>`<label class="p3-activation-reason"><input type="radio" name="p3-activation-reason" data-reason-code="${esc(value)}"><span>${label}</span></label>`).join('');
  body.innerHTML=`<div class="p3-activation-confirm"><b>¿Marcar este servicio como ACTIVADO?</b><p>${detail}</p><fieldset><legend>Motivo de cancelación</legend>${choices}</fieldset></div>${previewBody(s)}`;
  actions.innerHTML=`<button class="p3-preview-primary" type="button" onclick="abrirPreviewServicio('${esc(id)}')">Volver</button><button class="p3-preview-secondary danger" type="button" data-id="${esc(id)}" onclick="marcarServicioActivado(this.dataset.id)">Confirmar activado</button>`;
}
async function markActivated(id){
  if(P3.activationInFlight){notify('La activación ya se está confirmando. Esperá un momento.','info');return false}
  const reasonCode=document.querySelector('input[name="p3-activation-reason"]:checked')?.dataset?.reasonCode;
  if(!reasonCode){notify('Seleccioná el motivo de cancelación','warning');return false}
  const button=document.querySelector('#p3-preview-actions [data-id]');P3.activationInFlight=true;if(button){button.disabled=true;button.textContent='Confirmando…'}
  try{const {data,error}=await db().rpc('mark_driver_operator_service_activated_v2',{p_service_id:id,p_reason_code:reasonCode,p_reason_detail:null});if(error)throw error;if(data?.status!=='cancelled'||data?.business_status!=='activated')throw new Error('El servicio no confirmó el estado ACTIVADO');P3.queue=P3.queue.filter(s=>String(s.service_id)!==String(id));closePreview();render();notify('Servicio marcado como ACTIVADO','success');await loadQueue({silent:true});return true}catch(e){notify(e.message||'No se pudo marcar el servicio como ACTIVADO','error');if(button){button.disabled=false;button.textContent='Confirmar activado'}return false}finally{P3.activationInFlight=false}
}
function findService(id){return P3.queue.find(x=>String(x.service_id)===String(id))||null}
function setValue(id,value){const el=document.getElementById(id);if(!el||value==null||value==='')return;el.value=String(value);el.dispatchEvent(new Event('input',{bubbles:true}));el.dispatchEvent(new Event('change',{bubbles:true}))}
function prefillRemito(s){setValue('rem-nro-prestadora',s.service_order_number||s.service_number);setValue('rem-patente',s.vehicle_plate);setValue('rem-marca-modelo',s.vehicle_make_model);setValue('rem-origen',s.origin);setValue('rem-destino',s.destination);setValue('rem-cliente',s.customer_name);setValue('rem-telefono',s.customer_phone);const type=document.getElementById('rem-tipo-servicio');if(type&&s.concept_name){const match=[...type.options].find(o=>o.value.toLowerCase()===String(s.concept_name).toLowerCase()||o.textContent.toLowerCase().includes(String(s.concept_name).toLowerCase()));if(match)type.value=match.value}const marker=document.getElementById('phase3-remito-context')||document.createElement('div');marker.id='phase3-remito-context';marker.className='p3-remito-context';marker.innerHTML=`<b>${esc(s.service_order_number||s.service_number)}</b> · Servicio asignado`;document.getElementById('remitos-nuevo')?.prepend(marker)}
async function restoreServiceDraft(s){
  if(!s?.service_id)return false;
  const {data,error}=await db().rpc('get_driver_operator_service_remito_draft_v1',{p_service_id:s.service_id});if(error)throw error;if(!data)return false;
  setValue('rem-nro',data.nro_remito);setValue('rem-patente',data.vehicle_plate);setValue('rem-marca-modelo',data.vehicle_make_model);setValue('rem-origen',data.origin);setValue('rem-destino',data.destination);setValue('rem-km',data.km_reales);setValue('rem-cliente',data.customer_name);setValue('rem-cuit',data.customer_document);setValue('rem-telefono',data.customer_phone);setValue('rem-observaciones',data.observations);
  await window.AuxiliosRemitoAddonsV2?.restore?.(data.addons||null);return true;
}
function extraFieldMarkup(meta){return meta.type==='textarea'?`<label><span>${esc(meta.label)} *</span><textarea id="${meta.id}" rows="2"></textarea></label>`:`<label><span>${esc(meta.label)} *</span><input id="${meta.id}" type="${meta.type}" autocomplete="off"></label>`;}
async function renderRequiredCompletion(s){const root=document.getElementById('remitos-nuevo');if(!root||!s?.service_id)return;document.getElementById('phase3-service-required')?.remove();try{const {data,error}=await db().rpc('validate_operator_service_required_fields_v2',{p_service_id:s.service_id,p_overrides:overridesFromRemito()});if(error)throw error;const missing=Array.isArray(data?.missing)?data.missing:[],extras=missing.map(label=>EXTRA_FIELDS[label]).filter(Boolean);if(!extras.length)return;const box=document.createElement('section');box.id='phase3-service-required';box.className='p3-required-card';box.innerHTML=`<div><b>Completá antes de firmar</b><small>Estos campos fueron definidos como obligatorios en Configuración → Servicios → Formulario.</small></div><div class="p3-required-grid">${extras.map(extraFieldMarkup).join('')}</div>`;document.getElementById('phase3-remito-context')?.after(box)}catch(e){notify(e.message||'No se pudieron validar los campos obligatorios','error')}}
async function openSignedEdit(id){
  try{const {data,error}=await db().rpc('get_driver_signed_remito_edit_v1',{p_service_id:id});if(error)throw error;if(!data?.can_edit)throw new Error('El remito ya no admite correcciones');P3.selectedServiceId=id;P3.signedEditData=data;sessionStorage.removeItem('auxilios_driver_ad_hoc_mode');sessionStorage.setItem('auxilios_phase3_service_id',id);sessionStorage.setItem('auxilios_phase3_signed_edit','1');await window.AuxiliosRemitoAddonsV2?.loadReference?.();window.goTo?.('remitos');window.showRemitosView?.('nuevo');setTimeout(()=>void(async()=>{try{window.AuxiliosRemitoMobileV3?.setAdHocMode?.(false);window.AuxiliosRemitoMobileV3?.setSignedEditMode?.(true,data);window.remWizardReset?.();await window.AuxiliosRemitoAddonsV2?.restore?.(data.addons||null);setValue('rem-observaciones',data.remito?.observations);window.remWizardAbrirEdicionFirmada?.();window.AuxiliosRemitoMobileV3?.syncEvidence?.()}catch(inner){notify(inner.message||'No se pudo preparar la edición','error')}})(),120)}catch(e){notify(e.message||'No se pudo abrir el remito firmado','error')}
}
async function saveSignedEdit(){
  if(P3.correctionInFlight)return false;const serviceId=sessionStorage.getItem('auxilios_phase3_service_id');if(!serviceId||sessionStorage.getItem('auxilios_phase3_signed_edit')!=='1')return false;
  const validation=window.AuxiliosRemitoAddonsV2?.validate?.();if(validation&&!validation.ok){notify(validation.errors?.[0]||'Revisá peajes y excedentes','warning');return false}
  const button=document.getElementById('rem-btn-next');P3.correctionInFlight=true;if(button){button.disabled=true;button.textContent='Guardando…'}
  try{const operationId=crypto.randomUUID();const bundle=window.AuxiliosRemitoAddonsV2?.collect?.()||{payload:{addons_version:2,tolls:[],excesses:[],evidence:[]},files:[]};const payload=await window.AuxiliosRemitoAddonsV2?.uploadEvidence?.(bundle,operationId)||bundle.payload;payload.observations=document.getElementById('rem-observaciones')?.value||'';const {data,error}=await db().rpc('update_driver_signed_remito_v1',{p_service_id:serviceId,p_payload:payload,p_client_operation_id:operationId});if(error)throw error;if(data?.remito_status!=='firmado')throw new Error('No se confirmó la corrección del remito');sessionStorage.removeItem('auxilios_phase3_signed_edit');sessionStorage.removeItem('auxilios_phase3_service_id');P3.selectedServiceId=null;P3.signedEditData=null;window.AuxiliosRemitoMobileV3?.setSignedEditMode?.(false);window.showRemitosView?.('lista');notify('Cambios guardados. El remito volvió a revisión.','success');await loadQueue({silent:true});return true}catch(e){notify(e.message||'No se pudieron guardar los cambios','error');return false}finally{P3.correctionInFlight=false;if(button){button.disabled=false;button.textContent='Guardar cambios'}}
}
async function openRemito(id){const s=findService(id);if(!s)return notify('El servicio ya no está disponible','error');if(s.remito_action==='edit'||s.can_edit_remito)return openSignedEdit(id);if(!(s.remito_action==='complete'||s.can_complete_remito))return notify('Este remito ya no se puede completar','warning');try{if(!s.trip_id){const {data,error}=await db().rpc('ensure_operator_service_trip_v2',{p_service_id:id});if(error)throw error;s.trip_id=data?.trip_id||s.trip_id}P3.selectedServiceId=id;P3.signedEditData=null;sessionStorage.removeItem('auxilios_phase3_signed_edit');sessionStorage.removeItem('auxilios_driver_ad_hoc_mode');sessionStorage.setItem('auxilios_phase3_service_id',id);window.AuxiliosRemitoMobileV3?.setSignedEditMode?.(false);await window.AuxiliosRemitoAddonsV2?.loadReference?.();if(typeof window.goTo==='function')window.goTo('remitos');if(typeof window.showRemitosView==='function')window.showRemitosView('nuevo');setTimeout(()=>void(async()=>{try{window.remWizardReset?.();window.AuxiliosRemitoMobileV3?.setAdHocMode?.(false);prefillRemito(s);await restoreServiceDraft(s);await renderRequiredCompletion(s)}catch(error){notify(error.message||'No se pudo recuperar el borrador del remito','error')}})(),120)}catch(e){notify(String(e.message||e).replace(/^.*(?:JORNADA_REQUERIDA|VIAJE_EN_CURSO):\s*/i,''),'error')}}
async function openAdHocRemito(){try{P3.selectedServiceId=null;P3.signedEditData=null;sessionStorage.removeItem('auxilios_phase3_signed_edit');sessionStorage.removeItem('auxilios_phase3_service_id');sessionStorage.setItem('auxilios_driver_ad_hoc_mode','1');window.AuxiliosRemitoMobileV3?.setSignedEditMode?.(false);await window.AuxiliosRemitoAddonsV2?.loadReference?.();if(typeof window.goTo==='function')window.goTo('remitos');if(typeof window.showRemitosView==='function')window.showRemitosView('nuevo');setTimeout(()=>{window.remWizardReset?.();window.AuxiliosRemitoMobileV3?.setAdHocMode?.(true);const root=document.getElementById('remitos-nuevo');if(!root)return;const marker=document.getElementById('phase3-remito-context')||document.createElement('div');marker.id='phase3-remito-context';marker.className='p3-remito-context';marker.innerHTML='<b>Servicio sin asignación</b> · Quedará pendiente de vinculación por Operaciones';root.prepend(marker)},120)}catch(e){notify(e.message||'No se pudieron cargar peajes y excedentes','error')}}
function overridesFromRemito(d={}){return{customer_name:d.cliente||document.getElementById('rem-cliente')?.value||'',customer_phone:d.telefono||document.getElementById('rem-telefono')?.value||'',customer_email:d.email||document.getElementById('phase3-service-email')?.value||'',vehicle_plate:d.patente||document.getElementById('rem-patente')?.value||'',vehicle_make_model:d.marca||document.getElementById('rem-marca-modelo')?.value||'',origin:d.origen||document.getElementById('rem-origen')?.value||'',destination:d.destino||document.getElementById('rem-destino')?.value||'',operator_notes:d.observaciones||document.getElementById('phase3-service-operator-notes')?.value||document.getElementById('rem-observaciones')?.value||'',driver_instructions:document.getElementById('phase3-service-driver-instructions')?.value||'',purchase_order_number:document.getElementById('phase3-service-purchase-order')?.value||''}}
async function completeBeforeSignature(serviceId,d){const payload=overridesFromRemito(d),{data,error}=await db().rpc('complete_driver_operator_service_fields_v2',{p_service_id:serviceId,p_payload:payload});if(error)throw error;if(!data?.valid){const missing=Array.isArray(data?.missing)?data.missing:[];throw new Error(`Antes de firmar completá: ${missing.join(', ')}`)}return data}
async function confirmSignedArrival(s){const serviceLabel=s?.service_order_number||s?.service_number||'—',message=`Servicio: ${serviceLabel}\n¿Confirmás la firma del socio y el cambio de estado a ARRIBADO?`;const ask=window.OperatorServiceLifecycleV2?.confirmAction;if(ask)return await ask({title:'Confirmar firma y ARRIBADO',subtitle:'La firma quedará vinculada al remito y no podrá reemplazarse desde Operaciones.',message,confirmLabel:'Confirmar firma'});if(typeof window.confirm==='function')return window.confirm(message);throw new Error('No se pudo confirmar la firma dentro de AuxiliOS')}
function wrapRemitoSave(){if(window.__phase3RemitoWrappedV2||typeof window.guardarRemitoCompleto!=='function')return false;const original=window.guardarRemitoCompleto;window.guardarRemitoCompleto=async function(datosRemito){const serviceId=sessionStorage.getItem('auxilios_phase3_service_id');if(!serviceId)return original.apply(this,arguments);const signed=!!datosRemito?.firmaDataURL||(typeof hasSig!=='undefined'&&hasSig),isOnline=navigator.onLine;try{if(signed&&isOnline){await completeBeforeSignature(serviceId,datosRemito);const s=findService(serviceId);if(!await confirmSignedArrival(s))return false}datosRemito.operator_service_id=serviceId;const result=await original.apply(this,arguments);if(!result)return result;if(signed&&isOnline){sessionStorage.removeItem('auxilios_phase3_service_id');P3.selectedServiceId=null;document.getElementById('phase3-service-required')?.remove();notify('Firma registrada. Servicio ARRIBADO.','success')}else if(signed){notify('Firma guardada. Pendiente de sincronización para confirmar ARRIBADO.','info')}await loadQueue({silent:true});return result}catch(e){notify(e.message||'No se pudo guardar el remito','error');return false}};window.__phase3RemitoWrappedV2=true;return true}
function setView(view){P3.view=view==='history'?'history':'active';render();if(P3.view==='history'){if(typeof window.cargarRemitos==='function')Promise.resolve(window.cargarRemitos()).catch(e=>notify(e.message||'No se pudo actualizar el historial','error'));return}loadQueue()}
function startPolling(){clearInterval(P3.timer);if(!isDriver())return;P3.timer=setInterval(()=>{if(document.visibilityState==='visible')loadQueue({silent:true})},30000)}
function init(){if(P3.initialized||!db()||!profile())return false;P3.initialized=true;injectAssets();hookNavigation();injectPanel();let attempts=0;const wrap=setInterval(()=>{if(wrapRemitoSave()||++attempts>60)clearInterval(wrap)},250);if(isDriver()){loadQueue({silent:true});startPolling();document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')loadQueue({silent:true})})}return true}
Object.assign(window,{actualizarServiciosAsignados:()=>loadQueue(),abrirPreviewServicio:openPreview,cerrarPreviewServicio:closePreview,confirmarServicioActivado:confirmActivated,marcarServicioActivado:markActivated,abrirRemitoServicio:openRemito,abrirRemitoSinAsignacion:openAdHocRemito,guardarEdicionRemitoFirmado:saveSignedEdit,cambiarVistaServiciosChofer:setView,obtenerServicioAsignadoRemito:()=>findService(sessionStorage.getItem('auxilios_phase3_service_id'))});
const boot=setInterval(()=>{if(init())clearInterval(boot)},250);setTimeout(()=>clearInterval(boot),30000);
})();
